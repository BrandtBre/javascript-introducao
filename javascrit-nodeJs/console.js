// log -> registro

const minhaVar = true;

console.log(123);
console.log("texto");
console.log(minhaVar);

// tratamento de erro
console.error('erro');

console.table(); // para visualizar de forma mais organizada informações tabulares.

console.time(); // para temporizar período que uma operação de código leva para ser iniciada e concluída.
console.timeEnd();

console.trace(); // para exibir a 'stacktrace' de todos os pontos (ou seja, os arquivos chamados)
// por onde o código executado passou durante a execução.